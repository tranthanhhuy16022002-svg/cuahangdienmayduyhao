 "use client";
import { useStore } from "./StoreProvider";
export default function Cart(){const {cart,remove,clear}=useStore(); const total=cart.reduce((s,i)=>s+i.price*i.qty,0); return <main className="container section"><h1>Giỏ hàng</h1>{!cart.length?<div className="empty"><p>Giỏ hàng đang trống.</p><a className="btn blue" href="/san-pham/">Tiếp tục mua hàng</a></div>:<div className="cart-layout"><section>{cart.map(i=><div className="cart-row" key={i.slug}><div className="cart-thumb" style={{backgroundImage:`url(${i.image})`}}/><div><b>{i.name}</b><p>{i.price.toLocaleString("vi-VN")}đ × {i.qty}</p></div><button onClick={()=>remove(i.slug)}>− 1</button></div>)}</section><aside className="summary"><h2>Tạm tính</h2><strong>{total.toLocaleString("vi-VN")}đ</strong><p>Phí giao hàng sẽ được xác nhận theo khu vực.</p><button className="btn yellow full" onClick={()=>alert("Demo: bước tiếp theo sẽ kết nối form đặt hàng + database.")}>Đặt hàng</button><button className="btn outline full" onClick={clear}>Xóa giỏ hàng</button></aside></div>}</main>}