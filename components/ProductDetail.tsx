 "use client";
import { useStore } from "./StoreProvider";
import type { Product } from "@/lib/data";
export default function ProductDetail({product:p}:{product:Product}){const {add}=useStore(); return <main className="container section"><div className="breadcrumbs">Trang chủ / {p.category} / {p.name}</div><div className="detail"><div className="detail-image" style={{backgroundImage:`url(${p.image})`}}></div><div><small>{p.brand}</small><h1>{p.name}</h1><div className="detail-price">{p.price.toLocaleString("vi-VN")}đ</div><del>{p.referencePrice.toLocaleString("vi-VN")}đ</del><p className="saving">Tiết kiệm {p.saving.toLocaleString("vi-VN")}đ ({p.discount}%)</p><ul className="benefits"><li>✓ Hàng chính hãng</li><li>✓ Bảo hành theo hãng</li><li>✓ Hỗ trợ giao hàng</li><li>✓ Tư vấn qua Zalo</li></ul><button className="btn yellow" onClick={()=>add(p)}>Thêm vào giỏ hàng</button><a className="btn outline" href="tel:0987123456">Gọi 0987 123 456</a></div></div><div className="spec"><h2>Thông tin sản phẩm</h2><p>{p.description}</p><div className="spec-grid">{Object.entries(p.specs).map(([k,v])=><div key={k}><b>{k}</b><span>{v}</span></div>)}</div></div></main>}